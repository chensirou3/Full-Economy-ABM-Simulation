/**
 * 地图画布组件
 * 处理 PixiJS 地图渲染的底层逻辑
 */

import React from 'react'

// 这个组件主要作为 WorldViewer 的辅助组件
// 实际的地图渲染逻辑已经集成在 WorldViewer 中

export const MapCanvas: React.FC = () => {
  return (
    <div className="map-canvas">
      {/* 地图渲染逻辑已集成到 WorldViewer 组件中 */}
    </div>
  )
}
